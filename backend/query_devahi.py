import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'krgiweb.settings') # Wait, I don't know the exact settings module.
# Let me look for manage.py or check settings module in manage.py.
