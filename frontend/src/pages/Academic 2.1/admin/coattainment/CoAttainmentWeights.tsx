import React from 'react';

export default function CoAttainmentWeights() {
  return (
    <div className="bg-white rounded-lg border p-6">
      <h2 className="text-lg font-semibold mb-3">CO Attainment Weights</h2>
      <p className="text-sm text-gray-500">Weights UI placeholder — configure how configured columns map to final attainment.</p>
    </div>
  );
}
